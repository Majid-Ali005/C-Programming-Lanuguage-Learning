#include <stdio.h>

int main()
{
    //check number wether range in between 10,20
    int n = 11;
    if(n >= 10 && n<=20)
    {
        printf("%d number lies between [10,20]",n);
    }
    return 0;
}