#include <stdio.h>

int main()
{
    //iss ko khud se papaer pr explain kare.
    //Majid Ali
    int n = 30;
    if(n<10)
    {
        printf("%d < 10", n);
    }
    if(n > 20)
    {
        printf("%d < 20", n); // 30 < 20
    }
    return 0;
}