#include <stdio.h>

int main()
{
    int num = -4;
    if(num > 0)
    {
        printf("Number is positive %d",num);
    }
    if(num < 0)
    {
        printf("Number is Negative %d", num);
        //Output: Number is Negative
    }
    return 0;
}

